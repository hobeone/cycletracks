apt-get install subversion mysql-server libmysql-ruby libxml2-dev libsqlite3-dev libsqlite-dev build-essential
apt-get remove rubygems

http://rubyforge.org/projects/rubygems/

cd rubygems-1.3.1
sudo ruby setup.rb
sudo ln -sf /usr/bin/gem1.8 /usr/bin/gem

cd gcycle
sudo gem install -V -v=2.2.2 rails mongrel

sudo rake gems:install
rake db:migrate
rake db:fixtures:load
./script/server
